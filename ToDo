1. Plot SPEXone AOD and AE side by side, done make the same for oci but monthly
2. Make AOD and ( AE with SPEXone) around obilic take a certain radius and compute average by surfaces
    -> plot the curve as a function of time, and as a function of radius, do we see any changes when we increase the radius
    in theory it should change since the affect from the plant should change
    -> look also for specific modes
    -> check for all coal plants on the balkan
    retrieved by chat gpt. It has its data from https://www.gem.wiki/, https://justapedia.org/, https://en.wikipedia.org/


    -> add windspeed from previous time like a term for diffusion, use era5 data
    It seems that aod values in the surroundings are greater than at the emitting side. However AE seems to show a better signal of emissions.

Science related: population
  
TAKE quality flag into account!
3. Plot ERA5 data and oci aod next to each other is there any relationships?
4. Use earthcare data to look into vertical profile after a specific region is picked

5. Check how many data points are gathered relative to the area 


apptainer exec -B "$PWD":/work oceandata-notebooks.sif jupyter lab --ip 0.0.0.0 --no-browser --notebook-dir=/projects/0/prjs1474/aarifi/PACEfun/

apptainer exec \
  -B "$PWD":/work \
  -B "$PWD":/home/jovyan \
  --env HOME=/home/jovyan \
  --env SSL_CERT_FILE=/srv/conda/envs/notebook/lib/python3.11/site-packages/certifi/cacert.pem \
  --env REQUESTS_CA_BUNDLE=/srv/conda/envs/notebook/lib/python3.11/site-packages/certifi/cacert.pem \
  oceandata-notebooks.sif \
  jupyter lab --ip 0.0.0.0 --no-browser --notebook-dir=/projects/0/prjs1474/aarifi/PACEfun/

apptainer exec \
  -B "$PWD":/work \
  --env SSL_CERT_FILE=/srv/conda/envs/notebook/lib/python3.11/site-packages/certifi/cacert.pem \
  --env REQUESTS_CA_BUNDLE=/srv/conda/envs/notebook/lib/python3.11/site-packages/certifi/cacert.pem \
  oceandata-notebooks.sif \
  jupyter lab --ip 0.0.0.0 --no-browser --notebook-dir=/projects/0/prjs1474/aarifi/PACEfun/

apptainer exec \
  -B "$PWD":/work \
  --env SSL_CERT_FILE=/srv/conda/envs/notebook/lib/python3.11/site-packages/certifi/cacert.pem \
  --env REQUESTS_CA_BUNDLE=/srv/conda/envs/notebook/lib/python3.11/site-packages/certifi/cacert.pem \
  oceandata-notebooks.sif \
  jupyter lab --ip 0.0.0.0 --no-browser --notebook-dir=/projects/0/prjs1474/aarifi/PACEfun/


apptainer shell \
  -B "$PWD":/work \
  --env SSL_CERT_FILE=/srv/conda/envs/notebook/lib/python3.11/site-packages/certifi/cacert.pem \
  --env REQUESTS_CA_BUNDLE=/srv/conda/envs/notebook/lib/python3.11/site-packages/certifi/cacert.pem \
  oceandata-notebooks.sif \
  jupyter lab --ip 0.0.0.0 --no-browser --notebook-dir=/projects/0/prjs1474/aarifi/PACEfun/


cd /tmp
wget https://ftp.gnu.org/gnu/parallel/parallel-latest.tar.bz2
tar -xjf parallel-latest.tar.bz2
rm parallel-latest.tar.bz2
cd parallel-*
./configure --prefix=$HOME/.local
make && make install
export PATH=$HOME/.local/bin:$PATH 
cd /work

Next time resample with cdo!